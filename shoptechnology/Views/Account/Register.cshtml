@{
    ViewData["Title"] = "Đăng ký";
}

<div class="signup-hero p-4 p-md-5 mb-4 rounded-4" style="background:#111827;color:#fff;">
    <div class="row align-items-center">
        <div class="col-lg-5">
            <h2 class="display-6 fw-bold">Tham gia cùng chúng tôi</h2>
            <p class="lead-6 mt-3 mb-0">Truy cập hàng ngàn tài nguyên và mẫu thiết kế</p>
            <img class="img-fluid mt-4 rounded-4 shadow" src="~/img/best-tech-accessories.jpg" alt="Design resources" />
        </div>
        <div class="col-lg-7 mt-4 mt-lg-0">
            <div class="bg-white text-dark p-4 p-md-5 rounded-4 shadow-lg">
                <h4 class="mb-4">Tạo tài khoản</h4>
                <form method="post" novalidate>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label small text-muted">Họ và tên</label>
                            <input name="fullName" class="form-control form-control-lg rounded-pill"
                                placeholder="Nhập họ và tên" required />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label small text-muted">Tên hiển thị</label>
                            <input class="form-control form-control-lg rounded-pill" placeholder="Không bắt buộc" />
                        </div>
                        <div class="col-12">
                            <label class="form-label small text-muted">Email</label>
                            <input name="email" type="email" class="form-control form-control-lg rounded-pill"
                                required />
                        </div>
                        <div class="col-12">
                            <label class="form-label small text-muted">Số điện thoại</label>
                            <div class="input-group input-group-lg">
                                <select class="form-select rounded-start-pill country-code" style="max-width:140px">
                                    <option value="+1">🇺🇸 +1</option>
                                    <option value="+44">🇬🇧 +44</option>
                                    <option value="+81">🇯🇵 +81</option>
                                    <option value="+84" selected>🇻🇳 +84</option>
                                </select>
                                <input type="tel" class="form-control rounded-end-pill" placeholder="Your phone" />
                            </div>
                        </div>
                        <div class="col-12">
                            <label class="form-label small text-muted">Mật khẩu</label>
                            <div class="position-relative">
                                <input id="reg-pass" name="password" type="password"
                                    class="form-control form-control-lg rounded-pill pe-5" required />
                                <button type="button"
                                    class="btn btn-sm btn-link position-absolute top-50 end-0 translate-middle-y me-2 toggle-reg">Hiện</button>
                            </div>
                            <small class="text-muted">Ít nhất 8 ký tự, kết hợp chữ, số và ký hiệu</small>
                        </div>
                        <div class="col-12">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" checked />
                                <label class="form-check-label">Tôi đồng ý với <a href="#">Điều khoản</a> và <a
                                        href="#">Chính sách bảo mật</a></label>
                            </div>
                        </div>
                        <div class="col-12">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" checked />
                                <label class="form-check-label">Tôi đồng ý nhận thông tin cập nhật và ưu đãi.</label>
                            </div>
                        </div>
                        <div class="col-12 d-flex align-items-center gap-3 mt-2">
                            <button class="btn btn-lg rounded-pill px-5 signup-btn" type="submit">Đăng ký</button>
                            <span class="text-muted">Đã có tài khoản? <a href="@Url.Action("Login","Account")">Đăng
                                    nhập</a></span>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .signup-hero .shadow-lg {
            box-shadow: 0 16px 48px rgba(0, 0, 0, .25) !important
        }

        .login-btn {
            background: #d1d5db;
            border: 0;
            color: #111827
        }

        .login-btn:hover {
            background: #c9ced6;
            color: #111827
        }

        .lead-6 {
            font-size: 1.125rem;
            opacity: .9
        }

        .hero-geo {
            width: 100%;
            height: 260px;
        }

        .dots span {
            display: inline-block;
            width: 10px;
            height: 10px;
            border-radius: 999px;
            background: #6b7280;
            margin-right: 8px;
            opacity: .5
        }

        .dots span.active {
            opacity: 1;
            background: #ffffff
        }
    </style>
}


@section Scripts {
    <script>
        (function () {
            const btn = document.querySelector('.toggle-reg');
            const input = document.getElementById('reg-pass');
            if (btn && input) {
                btn.addEventListener('click', function () {
                    const isPw = input.type === 'password';
                    input.type = isPw ? 'text' : 'password';
                    btn.textContent = isPw ? 'Hide' : 'Show';
                });
            }
        })();
    </script>
}
